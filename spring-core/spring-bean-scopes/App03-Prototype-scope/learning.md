## Prototype scope

- For `@Scope("prototype")` specified spring bean , IOC container will create multiple objects , on the multple `getBean()` method call on container object

- **Prototype** is used when objects having different state
